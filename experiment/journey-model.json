{
  "schemaVersion": "emac.operator-model/v1",
  "modelId": "petclinic-owner-details",
  "initialRuntimeState": {
    "gateway-A": "CLOSED",
    "gateway-B": "CLOSED"
  },
  "declared": {
    "eligibleRequest": "GET /api/gateway/owners/{ownerId}",
    "prefix": "Gateway -> Customers succeeds and getOwnerDetails decision is reached",
    "operator": {
      "id": "getOwnerDetails",
      "type": "circuit-breaker",
      "primaryEdge": "Gateway -> Visits",
      "fallback": "emptyVisitsForPets"
    },
    "journeys": {
      "owner-history": {
        "ownerId": 6,
        "expectedVisitId": 1,
        "fallbackSatisfies": false,
        "target": 0.995
      },
      "owner-only": {
        "ownerId": 6,
        "fallbackSatisfies": true,
        "target": 0.995
      }
    }
  },
  "scopedAssumptions": [
    "For the seeded workload, a successful Customers response contains ownerId 6.",
    "For the seeded workload, a successful permitted Visits response contains visitId 1.",
    "The owner-history fallback does not satisfy the journey; the owner-only fallback does."
  ]
}

